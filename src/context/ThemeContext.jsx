import { createContext } from "react";

// Context for reading theme
export const ThemeReadContext = createContext(null);

// Context for updating theme
export const ThemeDispatchContext = createContext(null);